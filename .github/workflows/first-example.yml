name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My first Github Actions Job"
        
        - name: list files
          run: ls
        
        - name: path of my dir
          run: pwd
          
        - name: cat files
          run: cat README.md
